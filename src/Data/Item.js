class Item{
    Item(name, category, image, price, discount, rating){
        name=this.name
        category=this.name
        image=this.image
        price=this.price
        discount=this.discount
        rating=this.rating
    }

    // getDiscountedPrice = ()=>{
    //     return this.price*(1-(this.discount/100.0))
    // }
}

export default Item;